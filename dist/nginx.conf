daemon off;

pid	  nginx.pid;
error_log nginx.err;

user nobody nogroup;


events { worker_connections 64; }


http {
	server {
		access_log off;
		listen *:80;
		location / {
			proxy_pass http://localhost:5555;
		}
	}
}
